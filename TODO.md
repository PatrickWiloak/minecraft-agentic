# TODO

Working task list for **minecraft-agentic**. Read this at the start of a work session and keep it current as work completes - check items off with a date, add follow-ups as they surface. Stale TODOs are worse than none. Security debt (if any) is tracked separately in `SECURITY-DEBT.md`.

---

## Open

- [ ] Consider giving the critic more than one round (it currently critiques once and applies one patch). Worth it only if the single pass proves to help - measure before adding cost.
- [ ] 🟠 **A `window` op that omits `z1` is dropped, and the model omits it.** Found during the live
      confirmation below: two of the watchtower's windows were refused with `missing or non-numeric
      "z1"`, so the tower came out with fewer windows than designed. The door is behaving correctly
      (guessing a bound is how a `box` eats the plot), but a window is naturally a *flat rectangle*
      and a model writing one reasonably gives three coords and a plane. Consider letting `window`
      (and only `window`) default a missing second bound to its first, i.e. treat `z1` as `z0` -
      that is unambiguous for a plane and cannot run away. Cheap, and it recovers real windows.
- [ ] 🟠 **Stray mason blocks land on the roof slope.** The critic caught it unprompted on the live
      run ("stray stone brick blocks on the northwest slope, interrupting the wooden roofline", 8/10)
      and it is visible in the screenshots as pale specks across the shingles. Likely the mason's
      `cyl` running past the eaves into the carpenter's cone. Worth reproducing on the preset
      simulator, which already models exactly this class of cross-role overwrite.
- [ ] The critic gives a score, so build quality is now a number we can move. Next lever if it's wanted: the model still places some loose decoration in mid-air via `put` (glass/lanterns on nothing). A support check on decorator `put` ops, or a second critic round, would catch it.
- [ ] `npm run record` doesn't know about the review phase - a recorded clip cuts at "build done" and would miss a critic patch landing. Fine today (the recorder builds presets, which are never reviewed), but wire it up if the hero clip ever uses a prompt build with `CRITIC=on`.
- [ ] Decide whether the other scenes should also lose their sheer platform edges (a sloped grass/stone skirt would work the same way as the beach apron; only Beach has `shore: true` today).
- [ ] Consider surfacing the placement marker for the AI path too - right now a hand-placed spot is consumed by one build; a "keep this spot" toggle would let you stack several builds on one plot deliberately.
- [ ] Verify one live Claude and OpenAI build once keys are available. The code paths have still
      never been exercised against the real APIs, but they are no longer *wrong*: the Claude pin
      moved off a deprecated model to `claude-opus-5`, text extraction is block-type-aware (thinking
      is on by default there, so `content[0]` is not the answer), and both providers now name a
      truncation as a truncation. See the Done entry below. **Decision 2026-07-30: NOT a launch
      blocker** - the owner doesn't use paid keys, so this ships as-is with honest labeling instead
      (README note + docs/SETUP.md "battle-tested note": Gemini has the live mileage, Claude/OpenAI
      are test-pinned, please file issues). First real-world exercise will come from users.
- [ ] Consider a hand-driven OBS capture of the hero clip if even higher fidelity is wanted - `npm run record` is headless (software-rendered), so the grass texture carries some compression noise at 800px.
- [ ] Upload the social preview image - the one pre-public task only the repo owner can do in
      the GitHub UI (Settings -> Social preview; `docs/media/crew-castle-cherry-1600x900-30JUL2026.png`
      is the right shape). Link shares show the avatar until this is done.
- [ ] Consider a shareable/hosted-tunnel option (e.g. one-liner to expose the local viewer) - only if there's real demand; keeps the "100% local" promise by default.
- [ ] Draft launch post (HN / Reddit r/Minecraft+r/programming / X) - the demo clip now exists.

## Done

- [x] ~~**Post-publish doc audit + SEO pass.** Gaps found and fixed: the README Contributing
      section predated CONTRIBUTING.md and never linked it (or SECURITY.md); the loading-screen
      copy in README/SETUP described only the boot steps, not the 3D pane's mesh-progress
      preloader; LICENSE still named Patrick personally while package.json and the repo owner
      say Nobler Works. Verified all media references resolve and all README anchors have
      targets (the camera-bot one is an explicit `<a name>`, not a heading - fine). SEO: repo
      description rewritten keyword-rich, topics 9 -> 18 (nodejs/javascript/ai/bot/generative-ai/
      minecraft-bot/agents/openai/game-ai), package.json keywords widened to match, and the
      README got a natural-language keyword line under the tagline (multi-agent AI Minecraft
      builder / LLM / Mineflayer) since Google indexes GitHub READMEs~~ ✅ done 2026-07-30

- [x] ~~**Transferred to the org: github.com/NoblerWorks-HQ/minecraft-agentic** (same home as
      IRONSIGHT), a few minutes after the public flip. Topics, homepage, wiki/projects-off, and
      private vulnerability reporting all survived the transfer; repo URLs in package.json /
      README badge / SECURITY.md repointed; local remote updated; CI green under the org. GitHub
      permanently redirects the old PatrickWiloak URLs~~ ✅ done 2026-07-30

- [x] ~~**PUBLISHED PUBLIC 2026-07-30.** History squashed to a single initial commit before the
      flip (the old log carried "WIP updates", graphify add-then-remove churn, and "commit pending
      working-tree changes" - the project's real story lives in this file's Done log, which
      survives). Full-history secret scan was already clean; visibility flipped via
      `gh repo edit --visibility public` and private vulnerability reporting enabled (SECURITY.md's
      preferred channel). Remaining owner-only task filed above: the social preview image~~ ✅ done 2026-07-30

- [x] ~~**The 3D pane got a real preloader (gitgood-style), and the castle's "random purple
      blocks" became flags.** (1) The viewer pane used to hide its cover on the iframe's `load`
      event - 10-40s before the world actually meshed, so startup read as a frozen black pane.
      New `.vload` overlay in the panel: particle-constellation backdrop, wordmark, progress bar
      with % - and the progress is REAL: `src/viewer-hook.js` now also collects the viewer's
      three.js scenes on `window.__scenes` (pinned in `npm test`), and the parent page polls the
      biggest scene's child count (one child per meshed section; the sky's cube-map scene stays
      tiny so max() skips it). Stages: HTML -> `__cam` parked -> mesh asymptote; dismissed on
      settle (>=24 sections, 2.5s no growth) with a timeout so the viewer-unavailable fallback
      is never trapped behind it. Same overlay covers scene-travel reloads; boot screen gained
      the same particles + a step-driven crawling bar. Verified live headless: 27% "Meshing the
      world - 7 sections" mid-load -> dismissed at settle -> world fully revealed. (2) The purple
      blocks were the castle's pennants - `pick(red/blue/purple/yellow_wool)`, one lone cube on
      each spire cap. Now a pole + two cloth blocks flying INTO the plot, like the keep's flag;
      audit still clean~~ ✅ done 2026-07-30

- [x] ~~**Four new library presets - Toadstool Hollow (mushroom-cap cottage), The Old Oak Hideout
      (stilted treehouse), The Skylark (hot-air balloon), Pioneer Pad (rocket + gantry) - 9 -> 13,
      each audit-clean AND confirmed live with screenshots.** Same bar as the originals: every
      preset runs the parallel-physics simulator clean (60 seeds in `npm test`, spot-checked at
      200), and all four were then built through the real crew on the live server (1195 / 1037 /
      835 / 838 blocks, **0 missing each**) and photographed from three angles via `src/shot.js`.
      Looking at the pictures earned its keep twice, exactly as the critic history predicted:
      the treehouse's podzol-speckled lawn read as mud splats (now plain grass) and its canopy
      read as "a bush on a box" (now two r=7 layers + a dome, with the trunk-column skip bounded
      to the trunk's real top so the dome doesn't get a 2x2 hole). The audit caught its own bug
      pre-live: the flower scatter could land on the lamp-post cell and replace the post, popping
      the lantern (seeds 40,44-46). New-preset lessons already encoded: balloon/treehouse
      attachables use the pagoda's support-arrives-later pattern; the mushroom door reuses the
      wizard tower's mason-threshold trick; small mushrooms only plant on tracked mycelium/podzol
      cells (daylight rule the audit can't see). Menu/docs updated (listBuilds, README count +
      list, CLAUDE.md, .env.example LIBRARY_BUILD). Driver script pattern for future visual
      checks: build via the panel's POST /build, wait on /status, shoot via shootBuild - and mind
      the 3x3 grid: the 4th+ build on a full plot is refused, POST /clear first~~ ✅ done 2026-07-30

- [x] ~~**Open-source pre-flight: community files + GitHub repo settings.** Added `CONTRIBUTING.md`
      (setup, which test tier to run for which change, the untrusted-input and silent-failure ground
      rules), `SECURITY.md` (private-reporting channels + explicit threat model: offline-mode opped
      bots are the design, `expandOps`/`normalizePatch` bypasses are in scope), issue templates
      (bug report asks for the AI backend and the `verifyBuild` line, the thing that separates a
      broken build from a broken picture of it) and a PR template, plus `docs/ARCHITECTURE.md` - a
      front-door version of the pipeline (prompt -> ops -> expandOps -> crew -> repair/critic) that
      points into CLAUDE.md for the full invariants; README doc index updated to match. GitHub side
      via `gh repo edit`: 9 topics set (the package.json keywords), homepage -> noblerworks.com,
      wiki + projects tabs disabled (empty tabs on a fresh public repo read as neglect). Also
      audited for publish: `.env` never committed, secret-pattern scan across all 22 commits clean,
      `.env.example` values all blank, no absolute paths or client references in tracked files, CI
      green on latest push~~ ✅ done 2026-07-30

- [x] ~~**🔴 CLEARED: the `window` op is confirmed live - no floating glass.** Built "a small stone watchtower with a wooden roof and windows" through the real crew on the live Gemini path with the critic on, then photographed it from four angles and *looked* at the pictures, because the score cannot catch this (last time it scored the broken build 10/10). **The old bug is gone**: every pane is set into the wall, none hanging in mid-air outside it. Checked two ways that agree - geometrically, all **12 glass panes in the plan have a solid neighbour** (0 floating); and visually, three faces of the tower show glazed openings in the stonework and nothing suspended off it. The build: 2,913 blocks, 34 missing (all `oak_leaves`, foliage that pops on its own), critic **8/10**, and the repair loop's model step correctly diagnosed its one stuck block ("the supporting block beneath it was coarse_dirt placed by the same role") and patched it. Two follow-ups the run surfaced are filed above as 🟠 (windows dropped for a missing `z1`; stray mason blocks on the roof slope) - neither is the floating-glass bug~~ ✅ done 2026-07-29

- [x] ~~**README now documents the thing the project is actually about.** Every one of the README's mentions of "ops" was `docker/ops.json` (operator permissions); `src/ops.js` - the change that took AI builds from 74 blocks/3-10 to 2,773/10-10 - appeared nowhere, not even in the file tree. New section: why the presets were better (primitives, not intelligence), the ~11-tokens-per-block arithmetic that made enumeration unaffordable, the before/after table, why the real prize is that the old mistakes became *impossible*, the windows-as-one-op lesson, and the untrusted-input door. Also: the architecture diagram now shows ops flowing through `expandOps`; the file tree gains `ops.js`, `fill.js`, `json.js`, `profiles.js` and `library/canvas.js`; `npm run test:ops` is listed; and the "details that took the longest" section gains the `/fill` cap story. All internal links verified~~ ✅ done 2026-07-29

- [x] ~~**"The model did not return valid JSON" now says WHICH failure it is.** Hit live during the window-op confirmation: a coordinator run died on that message, and the diagnostic printed the *first* 200 characters - which is exactly the part that looks identical whether the reply was truncated, refused, or prose. The tail is what tells you. `diagnose()` (`src/json.js`) now counts unclosed structure *outside of strings* and reports "it is TRUNCATED, not refused - 3 unclosed levels after 8,412 characters. Raise maxTokens" versus "it does not start with { or [" versus "balanced but unparseable", each with the offending end of the reply. This is the third place the same lesson has had to be learned (Gemini's MAX_TOKENS, then Claude/OpenAI's, now the parser itself): **a truncation must never be reported as a refusal.** 8 checks in `npm run test:loops`, including that braces inside strings are not counted as structure~~ ✅ done 2026-07-29

- [x] ~~**`@anthropic-ai/sdk` 0.52.0 -> 0.115.0, and the dependency tree is back to 0 vulnerabilities.** The pin was ~60 minor versions behind, on the one provider path that had never been exercised. Only one call site touches the SDK (`completeClaude`), and the shapes it uses are unchanged - verified directly that the dynamic `import()`'s default export still constructs, `client.messages.create` is still there, and the typed error classes still hang off the constructor. The bump surfaced two newly-published advisories in **pre-existing transitive** deps, neither from the SDK: `brace-expansion` (**high** - DoS via exponential `{}` expansion, via nodemon -> minimatch) and `body-parser` (low, via prismarine-viewer -> express). The high one would have failed CI's `npm audit --audit-level=high` on the next run regardless of this work; `npm audit fix` cleared both without touching a direct dependency. Re-verified after: full `npm test` green, and `npm run e2e` live (viewer serving HTTP 200, block placed and read back) since the express/body-parser bump sits under the viewer~~ ✅ done 2026-07-29

- [x] ~~**Four silent-failure classes closed at once - the two that could only bite a paying provider, the one that let a model patch place blocks the server discards, and the one that made "let me clear the area first!" do nothing on big builds.** (1) **The Claude path was pinned to a deprecated model and had never run.** `claude-sonnet-4-20250514` -> `claude-opus-5`; and a straight swap would have broken anyway, because thinking is ON BY DEFAULT on that family, so the first content block is a `thinking` block and the old `res.content[0].text` was `undefined` - which every parser downstream reports as "the model did not return valid JSON", i.e. as a refusal. Text extraction now takes the first `type === 'text'` block. (2) **The truncation invariant was enforced for Gemini only.** The repo has documented "a truncated reply reads as a refusal" as a top-level invariant since the 4096-token outage, and `completeGemini` raised `finishReason=MAX_TOKENS` as its own error - while `completeClaude` never checked `stop_reason` and `completeOpenAI` never checked `finish_reason`. Both now do (plus Claude's `stop_reason=refusal`, checked *before* content is read, since a decline is a 200 with empty content), via exported `extractClaudeText`/`extractOpenAIText` that `npm run test:loops` pins without a key. `completeVision`'s default `maxTokens` also went 4096 -> 16384 - the exact number that caused the outage, still sitting as the default in the one function the invariant was written about. (3) **`normalizePatch` skipped the door `expandOps` enforces.** A repair or critic patch had its `minecraft:` prefix stripped and nothing else: a hallucinated `wooden_plank` went out as `/setblock ... minecraft:wooden_plank`, which the server discards without a word, so the repair "succeeded" and the hole stayed - and an out-of-range coordinate was never clamped, so a fix could land on the neighbouring plot. Both now go through `normalizeType` (real 1.20.1 registry, repairs `stone_brick` -> `stone_bricks`) and `LIMITS`, clamped in the relative frame before the absolute conversion. (4) **The crew's area-clear was one `/fill` over the plan's whole bounding box, and `/fill` refuses anything over 32,768 blocks silently.** Every preset fits (castle: 20,808), which is why it survived; an ops build may legally span the plot to y=48 = **122,112 blocks**. Proved live by marking both corners of such a region: after the old single `/fill` **both markers still stood**; after the new `fillRegion` (4 commands) both were air. New `src/fill.js` owns the split - volume-halving, so a long thin shore ring stays one command - and `crew.js`, `demo.js`, `offline-demo.js` and `web.js` (`fillRegion`, `fillBiome`, `levelSite`) all use it instead of four hand-rolled copies. Also: `demo.js` cleared from `origin.y` up, leaving any foundation course at y=-1 standing; and `parsePlan`/`parseJson` were byte-identical copies of the same tolerant parser, now one `src/json.js`. 15 new checks in `npm run test:loops` (patch hardening, both providers' truncation/refusal handling, and the fill splitter's cap/coverage/no-overlap properties); full `npm test` green, and a live `npm run replay` built 813 blocks with 0 missing through the new clear path~~ ✅ done 2026-07-29

- [x] ~~**The AI builds were bad for a structural reason, not an intelligence one: we asked the model to hand-type a bitmap while the presets got to write vector graphics. Now the model designs in OPS.** A preset says `walls(...)` and gets 400 gap-free blocks; the model was asked for that same wall as one JSON object per block (~11 tokens each), so a 900-block build cost ~10k tokens of pure mechanical enumeration and the castle (3,338 blocks) ~38k - unbuyable at any budget. It shortcut, exactly as anyone would: **74 blocks, holes in the walls, 3/10 from our own critic.** Fixed by extracting the library's primitives (`src/library/canvas.js`) and giving the MODEL the same vocabulary (`src/ops.js`: box/walls/floor/cyl/disc/ring/cone/door/put/punch/window/scatter). ~28x compression, so scale is affordable and the model spends its budget on design. **Same prompt, measured live: 74 blocks / 3-10 -> 2,773 blocks / 10-10, 0 missing.** The real prize is that the old failure modes became impossible: a `walls` op is gap-free by construction, `door` always places both halves, `cone` orients its own shingles. Ops are untrusted input, so `expandOps` is the single door (clamps to the plot, refuses runaway ops, caps the total, coerces roles, and validates block names against the real 1.20.1 registry - repairing `stone_brick` -> `stone_bricks` and dropping hallucinations that `/setblock` would otherwise discard in silence). The coordinator's op reference is GENERATED from the OPS table so the prompt can't drift from the code, and the worked example (`src/plans/reference-ops.json`, shown on every request) is audited on the same parallel-physics simulator as the nine presets - which caught two real bugs in it before any model saw it. New `npm run test:ops` (25 checks). **Screenshotting the 10/10 build then found what the critic missed - panes of glass floating outside the tower** - so glazing became one `window` op that carves and glazes together and glazes only where a wall really was: if a rule needs two ops to agree, make it one op. That last part is unit-tested but not yet confirmed live (open 🔴)~~ ✅ done 2026-07-14

- [x] ~~**README hero media: replaced the timelapse webp with screenshots of finished builds** - the recorded clip read badly (software-GL headless frames crushed through 10fps lossy webp, and half the loop is a half-built site). New hero: Stonewatch Keep on Cherry Grove at 1600x900, plus a two-shot gallery (Vermilion Pagoda on Jungle; the beach plot with six builds standing). All are straight `page.screenshot()` captures of the live viewer after `verifyBuild`-clean crew builds, date-stamped in `docs/media/`; the old webp+mp4 deleted (recoverable from git history). `npm run record` stays for actual footage - README + CLAUDE.md now note that stills are the better-looking artifact. Capture gotcha for next time: the viewer's default orbit camera looks DOWN from 20-up/20-south at the camera bot on the ground, so tall builds (wizard tower, lighthouse) crop their tops off; wheel-zoom-out barely helps and drag-orbiting down flips the camera under the superflat ground (all-sky frames). A pose-forced capture (wrap `renderer.render` via the `__THREE_DEVTOOLS__` handshake, set camera position/lookAt every to-screen frame) frames them perfectly if tower/lighthouse shots are ever wanted~~ ✅ done 2026-07-14

- [x] ~~**Open-source pre-flight - the repo is publishable.** Verified from a CLEAN CLONE of the pushed master (not the working copy): `npm install` succeeds with 0 vulnerabilities, `postinstall` self-applies the prismarine-viewer patches, and the full `npm test` passes on Node 22 with no API key, no Minecraft server and no Docker. Full-history secret scan across every blob in every commit: clean, and `.env` has never been committed. Fixed: (1) **`graphify-out/` was tracked** - 2.1MB of internal knowledge graph, against the public-repo rule; now untracked + gitignored (and `.gitattributes`, whose only line was the graph's merge driver, deleted as dead config). It stays in history, which is FINE and deliberately not rewritten: every graph blob ever committed was scanned for personal paths, emails and keys and is clean, it describes code that is about to be public anyway, and the history bulk is the README's demo media (4.9MB webp), not the graph - a force-push rewrite would buy nothing and cost the cofounder's clone. (2) **No `engines`/`repository`/`author`/`keywords`** in package.json - a stranger on old Node got a cryptic native-module failure instead of "upgrade Node". Pinned `>=20`, and fixed the version drift it exposed: the README badge, README prereqs, `docs/SETUP.md` and `scripts/setup.js`'s own gate all still said 18. (3) **No CI** - added `.github/workflows/ci.yml` (Node 20 + 22: `npm ci` -> `npm test` -> `npm audit --audit-level=high`, all green locally) plus the badge. The suite needing no key/server/Docker is what makes this cheap~~ ✅ done 2026-07-14

- [x] ~~**The MODEL half of both loops is verified live - and getting there fixed two real bugs that had made it look like the model was refusing to answer.** (1) **One dead model was taking the whole product down.** Gemini's free tier retires and overloads models per-MODEL, not per-key: our pinned default `gemini-flash-latest` 503'd "high demand" for hours while the same key answered instantly elsewhere, `gemini-2.5-flash` had been closed to new keys entirely ("no longer available to new users"), and `gemini-2.0-flash` carried a free quota of 0. A Gemini call now walks a fallback chain (`GEMINI_FALLBACKS`) until one answers, sticky for the process so the dead-model probe is paid once, not per build; `providerLabel()` reports the model actually answering, not the one we asked for first. A 400/403 still surfaces immediately - `geminiModelIsUnavailable()` is the retry-vs-fail decision and both halves are pinned by tests. (2) **The repair and critic loops were silently truncating.** Both asked for JSON on a 4096-token budget, but a REASONING model (`gemini-3-flash-preview`) spends `maxOutputTokens` on thinking *before it writes a single character*, so the reply came back as half an object and every parser downstream reported the same useless thing: "the model did not return valid JSON". Raised both to 16384 (a ceiling, not a spend) and `completeGemini` now detects `finishReason=MAX_TOKENS` and says *truncated*, not *refused*. Verified end-to-end through the real panel path: a live coordinator plan ("Sentinel Spire", 74 blocks, chain fell through TWO dead models to `gemini-flash-lite-latest`); the repair loop's model step returning a correct diagnosis ("the door and path blocks lacked solid support at y=71") and a +3-block patch; and `completeVision` returning a real critique (3/10, four specific issues) whose +12-block patch was applied. A screenshot of the finished tower confirms the patch coordinates landed **ON the building** - the rel->abs conversion in `normalizePatch` is right on real model output, with no stray blocks out on the lawn~~ ✅ done 2026-07-14

- [x] ~~**Fix: "why are some builders floating?" - a browser tab can silently miss entity events, stranding builders drawn mid-air at stale positions** - server truth (rcon) had every bot parked on the ground while the browser drew some floating; an entity census of the live THREE scene (drawn meshes vs rcon, one long-lived tab across a whole build) reproduced it: one bot left drawn at a fractional mid-fall position 5+ seconds after the whole crew had teleported to formation. The socket's entity stream is complete at the source (a tap sees every final teleport, matching rcon to the decimal) - the loss is per-connection delivery during the build's block-update flood. Fix mirrors the camera-position re-announce: `startViewer()` re-emits `entityMoved` for every tracked entity every 2s, so a strand heals within 2s and a correct browser sees a no-op tween. Verified: post-fix census matches rcon exactly after a full build. Also: friendlier `EADDRINUSE` message in `scripts/web.js` (a busy :8080 now says "another panel is probably running" + the pkill line instead of a stack trace)~~ ✅ done 2026-07-14
- [x] ~~**Three ideas taken from other open-source Minecraft-agent projects, credited in the README's "Prior art & inspiration"** - (1) **Voyager**'s self-correction loop: `verifyBuild` already found the blocks that never landed and only ever printed the count; `src/repair.js` now re-places them, and when a block still won't stay (illegal, not dropped) it shows the model the failure *with the reason the game refused it* and applies the patch to the PLAN. (2) **APT**'s multimodal reflection: `src/critic.js` + `src/shot.js` photograph the finished build in a headless browser from three angles and hand a vision model the pictures next to `src/digest.js`'s ASCII floor maps of its own blueprint - the pairing is what makes the critique actionable in real coordinates. Opt-in (`CRITIC=on` / panel checkbox). (3) **mindcraft**'s agents-as-JSON: the four roles were hardcoded in `worker.js` AND re-described in the coordinator's prompt, and the two had drifted; they're now one file each in `src/profiles/`, feeding both, plus per-request few-shot example selection from the preset library. New: `npm run test:loops` (31 checks, no server/browser/key) pins patch normalization (rel->abs coords, dropped type-less entries), the repair loop's stop-when-a-round-fixes-nothing rule, the profile/library build-order agreement, the digest format, and the critic's framing. Verified live against the server: full crew builds through the new `finishBuild` path (1245 blocks, 0 missing), and `shootBuild` captures 3 real orbited PNGs of the build via Playwright + the live viewer. **Looking at those PNGs found a real bug**: the camera bot is parked at the site's CORNER during a build, so the first shots were an acre of empty grass with the tower half out of frame - and once centred horizontally, a 32-block tower still had its top half cut off, because the shot was aimed at its feet. `Crew.frameBuild()` now re-parks the camera on the plan's bbox centre in all three axes before shooting. (The model call itself was blocked by Gemini 503s at the time; verified the same day - see the entry above)~~ ✅ done 2026-07-14
- [x] ~~**Fix: "blocks flicker in and out during builds - the perimeter" - prismarine-viewer deletes chunk meshes the moment they slide out of the camera's radius, and this project now patches that out** - reproduced with a headless-browser frame-differ against the live panel (idle baseline: 0 changed pixels over 8s; the shore never moves while the camera holds still), then pinned by tapping the viewer's own socket stream: every build start hops the camera bot one grid cell (`aimCamera`), and one 40-block hop fired **17 `unloadChunk`s** - a whole strip of shore/water/horizon `scene.remove()`d on the spot at the trailing edge of the 16-chunk radius while the leading edge re-meshed in over the next seconds. `scripts/patch-viewer.js` (same install-time + startup self-heal mechanics as the stairs fix) now keeps stale meshes when a column leaves the radius - the world outside the active build site is static, so the last picture is the correct picture, replaced when the column re-enters range - and always applies late-arriving mesher geometry, which also removes the frozen-section mechanism the camera bot works around. Verified: `npm test` green (and now fails if the perimeter patch is missing), `npm run test:viewer` green (961 loads, 0 mid-build unloads, 774/774 blocks in the browser), scene switches (the 320-block camera jump, worst-case churn) render clean with no ghost or doubled geometry~~ ✅ done 2026-07-14
- [x] ~~**The world is now a raised superflat - underground caverns are gone from every preset/world** - normal terrain generated caves under every plot, and nothing in this project goes underground; the scenes' "solid encased base" only ever papered over the caves directly beneath a platform. `scripts/server.js` + `docker-compose.yml` now create the world with `LEVEL_TYPE=FLAT` and explicit `GENERATOR_SETTINGS` layers: bedrock -> deepslate -> stone -> dirt -> grass surface at exactly y=63 (a default superflat's y=-60 surface is below y=0, which the browser viewer never draws - the old "don't use superflat" note - and y=63 keeps the surface 1 above the panel's SEA_Y so shore scenes still flood). Required a world reset (approved); verified live: fresh flat world generated, Plains + Beach scenes constructed, six preset builds all "verified in-world" with 0 missing~~ ✅ done 2026-07-14

- [x] ~~**README rewritten around the panel, hero clip re-recorded with a repeatable `npm run record` - and two viewer bugs found while doing it** - the README still led with `npm run play` and never mentioned scenes, click-to-place or Clear ground; it now leads with `npm run web`, documents the panel, and carries a new section on the engineering that makes builds look like construction (route-walking, self-verifying builds, the preset simulator, the pacified world, the camera bot). New `scripts/record-demo.mjs` (`npm run record`, playwright as a devDependency) drives a headless browser against a running panel and cuts the two-speed timelapse itself: it frames the shot, builds, orbits for the reveal, and emits a date-stamped `.mp4` master plus the compressed `.webp` the README embeds (5.0MB / 20s; the 35MB first cut was not a README asset, it was a download). It **refuses to publish a clip of a build that didn't finish** - it had cheerfully cut a 40s timelapse of a castle that died at 829/1402 blocks and overwritten a good take with it. Recording it surfaced two real bugs, both fixed: (1) **the browser rendered an empty blue frame on any cold connect while the crew was idle** - prismarine-viewer pushes the camera position ONLY on a bot `move` event, and our camera bot exists precisely because it never moves, so a page that connected between builds never learned where to point; `startViewer()` now re-announces the position once a second (no chunk churn - `updatePosition` is a no-op unless the bot crosses a chunk boundary). (2) **the view was aimed at the corner of the plot, not at the build** - the shot was mostly empty platform with the castle at the edge of frame; `aimCamera()` now stands the camera in the middle of the cell about to be built on, re-aimed before each build (after levelling, before the first block). Also: playwright's screencast runs on its own timebase (a 200s session muxes to ~580s of video), so the wall-clock trim points were wrong and the "18s" cut came out at 52s - the recorder now maps wall time onto the recording's clock before trimming~~ ✅ done 2026-07-13

- [x] ~~**Fix: the "next free grid spot" was SESSION MEMORY, and a panel restart bulldozed four finished presets with it** - `nextOrigin()` picked the next 3x3 cell from `sites().length`, a counter that starts at zero every boot, while the world (and the builds on it) live on disk. Restart the panel over a full plot and the counter says "cell 0 is free": the next five builds `levelSite()`'d themselves straight through the castle, wizard tower, cottage and lighthouse (caught only because the gallery screenshot came up four buildings short). Exactly the class of bug the "Clear ground" fix documented - the world is the source of truth, session state is a cache - but the WRITE path hadn't gotten the lesson. `nextOrigin()` now probes each cell's core (a coarse 3-block lattice over 0..31 x/z, 24 blocks up, read from the lead bot's world copy - free) and takes the first cell with nothing standing on it; a full grid now REFUSES with "use Clear ground or Pick a spot" instead of silently wrapping onto cell 0 and demolishing it, and `runBuild` rejects requests that arrive before boot anchors `baseOrigin` (a mid-boot request used to crash on `undefined.x` - found when two panels were being traded mid-build). Verified live: with five builds standing, four rebuild requests landed on the four empty cells, nothing was overwritten, 9/9 gallery complete~~ ✅ done 2026-07-13
- [x] ~~**Five new library presets - Gristmill Rise (windmill), The Vermilion Pagoda, The Wandering Gull (galleon), Temple of the Golden Sun (ziggurat), Celestia Observatory - all built to the same audit-clean bar as the original four.** Each passes `npm run test:presets` across hundreds of seeded runs: openings punched (never overwritten), attachables placed so their support's arrival is the only neighbor event they see, gravity/soil rules respected, oriented stairs everywhere. New tricks the audit forced: a door whose mason support arrives too late (the pagoda plinth takes ~20s to walk) gets a same-role carpenter SILL instead - y-ascending makes it deterministic; a ship hull in water means the landscaper's lawn/water must skip the hull's exact footprint, not its bounding box; dune sand may drift against a ziggurat but never onto it (cross-role overwrite); `box()` gained function-palette support to match `walls()`/`floor()`. Block budgets 890-2,500 per preset, roles balanced. Panel/play menus pick them up automatically from `listBuilds()`. Verified live: all five built by the crew on the Plains plot (filling the 3x3 gallery to 9/9) with 0 missing blocks each~~ ✅ done 2026-07-13
- [x] ~~**Fix: STAIRS WERE NEVER RENDERED IN THE BROWSER - prismarine-viewer treats any block whose name contains "air" as air, and "stAIRs" matches** - the true root cause of "why do i see an unfinished building?". `getModelVariants` in prismarine-viewer 1.33.0 (viewer/lib/models.js and, critically, the prebuilt `public/worker.js` bundle the browser actually runs) starts with `if (block.name.includes('air')) return []` - meant for air/cave_air/void_air, but it also matches every one of the game's 53 stair blocks, so stairs meshed to ZERO geometry since the project began. A finished cottage rendered as a roofless shell with its slab ridge floating in mid-air (slabs render; the stairs under them don't), and the castle's tower cones showed as floating apex caps with pennants. The world was always right - `verifyBuild` 0 missing, scout bot read all 188 cone stairs in-world - only the picture was wrong; and `npm run test:viewer` couldn't catch it because it verifies the socket DATA stream, not the pixels. Proved with a minimal repro (planks meshed 24 vertices, stairs 0) and a 5-block test row in-world (planks + slab visible, 3 stairs invisible). Fix: `scripts/patch-viewer.js` rewrites the check to `name === 'air' || name.endsWith('_air')` in both the source and the shipped bundles - idempotent, wired into `postinstall`, self-healed at `startViewer()` time for existing installs, and `npm test` fails if the installed package is ever unpatched again. Verified live: test stairs render, castle wears four shingled cones, the cottage has a full pitched roof. NOTE: this bug also contaminated the 2026-07-12 "roofless house" diagnosis - the frozen-chunk-section bug was real and its camera-bot fix stands, but the roof would have been invisible either way~~ ✅ done 2026-07-13
- [x] ~~**"Perfect the presets": every library preset audited against the crew's REAL parallel schedule - dozens of invisible defects found and fixed, new `npm run test:presets` gate** - the trigger was "why do i see an unfinished building?" (a leftover from an earlier session - the world lives on disk, this panel had built nothing yet - made to look GUTTED by the invisible-stairs bug above), and "perfect them all" surfaced a whole class of defects `verifyBuild` can never see. The crew builds in PARALLEL with 3s staggered role starts (crew.js), so every "later role overwrites an earlier role's block" window/lamp was actually a RACE the mason usually won ~50s later: castle keep windows, tower arrow slits, gate corbels, lighthouse portholes and shaft lamps, wizard spiral windows - all stoned back over in live builds, while `verifyBuild` (which only checks for air) reported 0 missing. New `test/preset-audit.test.mjs` simulates the exact schedule (buildRoute order, 3s stagger, 100ms/block) with vanilla physics (neighbor-update pops, gravity, soil rules, door support, blocked-entrance checks) across seeded runs; it found ~145 distinct defects. Fixed: all openings now PUNCHED out of the mason's list (`canvas.punch()`) and owned by the decorator; the lighthouse door - which popped on 60/60 simulated runs (it floated over the carved island surface) and was ALSO walled in behind the flared base - now opens through the flare, level with the jetty, with lamp posts at the jetty's end; castle gate/battlement lighting rebuilt (wall torches on the outside face; torches ON merlons, clear of the tower cylinders); wizard soul lanterns moved from mid-air INSIDE the tower onto the trim ledge; cottage gables closed (open triangles under the roof) with attic windows, roof stairs given facings (default-north stairs rendered one slope as backwards steps - all cone roofs fixed too via oriented `cone()` + solid `STAIR_CAP` apexes so end rods/flags don't pop), ridge capped symmetrically on odd widths; the keep got a roof deck (its flagpole floated over an open top); floors no longer fight wall rings for rim coords; lighthouse tufts placed ON grass cells instead of replacing surface blocks (all of them sat on stone and popped); flower scatters keep to real soil, off paths/ponds/farmland and out of doorways. All four presets then rebuilt live through the panel and verified~~ ✅ done 2026-07-13
- [x] ~~**Fix: "Pick a spot" ate every click - two more silent killers in the click path** - the feature verified live 2026-07-12 and was dead again by the next session; this time not the camera hook's global but (1) **the drag guard armed on an event that never fires.** The page told a click from an orbit-drag by comparing the click position against the last `mousedown` - but the viewer's orbit controls `preventDefault()` their `pointerdown`, and per the Pointer Events spec a canceled pointerdown suppresses the compatibility `mousedown` entirely while `click` still fires. So the guard's start point stayed (0,0), every real click measured as a huge "drag", and the handler returned before logging anything. The guard now arms on `pointerdown`, which always fires. (2) **The sky was stealing the camera.** The viewer draws its environment through the SAME `renderer.render()` the hook wraps - a three.js `CubeCamera` pass rendering 6 faces to a cube render target with face cameras parked at the origin - so `window.__cam` flipped between the real camera and an identity camera every frame, and a pick unprojected through whichever won the race (`t = -245`, "aim at the ground"). The hook now parks the camera only when `getRenderTarget()` is null - the to-screen pass is the only one carrying the user's camera. Found and verified with a real headless Chromium driving the live panel: before, the iframe document received `pointerdown`/`click` but NO `mousedown` and made no `/place` request; after, a click → `POST /place` → "Next build staked out at (-166, -54)" → marker ring in-world, aim banner dropped. `npm test` pins all of it: the guard arms on pointerdown, no `mousedown` listener exists in the viewer doc, and an off-screen render pass must not steal the parked camera~~ ✅ done 2026-07-13
- [x] ~~**Fix: "Pick a spot" never worked in the browser, and "Clear ground" greyed itself out on a plot full of buildings** - two unrelated failures in the web panel, both silent. (1) **Click-to-place was dead on arrival.** The proxy's injected hook waited for prismarine-viewer to assign `window.THREE`, on the strength of the `global.THREE = require('three')` line in the package - but that line lives in `lib/index.js` / `lib/headless.js`, the NODE entries. The browser bundle keeps three.js in module scope and sets `window.THREE` exactly zero times (verified against the shipped `public/index.js`), so the setter never fired, `window.__cam` was never assigned, and `pickGround()` returned `null` on every click since the feature shipped - clicks landed, they just resolved to nothing. `npm test` was green throughout, because it handed `pickGround()` a camera AND a THREE and so mocked away the only broken part. Fixed by hooking three.js's devtools handshake instead (`window.__THREE_DEVTOOLS__` - every `WebGLRenderer` dispatches an `observe` event carrying itself; wrap its `render()` and the live camera arrives every frame), and `pickGround()` now does its vector math on the camera's own `Vector3` since there is still no THREE to construct one from. The test now runs the REAL hook against a fake renderer and asserts the installed bundle still dispatches to that global - the check that would have caught this. (2) **"Clear ground" cleared what the PROCESS remembered, not what the WORLD holds.** `state.sceneSites` is in-memory; the Minecraft world is on disk. Restart the panel, return to a plot covered in yesterday's builds, and `built` reported 0 - so the button disabled itself permanently and `/clear` refused with "nothing to clear". It now bulldozes the whole build ZONE (`buildArea()`) and is gated on whether the plot exists, not on session memory. Verified live end-to-end: hook present in the served page, `clearable: true` on a plot with `built: 0`, and a post-clear survey of the world (a read-only bot scanning 231x231 columns) found 0 blocks standing in the build zone, surface 100% grass, base intact - the scene's trees and flowers sit outside the zone by design and are left standing~~ ✅ done 2026-07-13
- [x] ~~**The crew now works the site instead of strobing across it, and the world stops ending 160 blocks out** - two things that made the viewer read as fake. (1) **Movement.** Workers hop to stand beside their work every 8 blocks, but the generators emit blocks in loop order - a whole wall, then a floor, then trim on the far side - so a bot spent the build teleporting back and forth across the plot. `buildRoute()` (`src/worker.js`) now sorts each role's list y-ascending and takes a nearest-neighbour walk through each layer: the castle mason's travel drops **10,215 -> 2,817 blocks**, the cottage carpenter's **1,603 -> 388**, and the hops become a shuffle along the course being laid. The y-ascending key is load-bearing, not cosmetic - it preserves the build timeline (support before supported, a door's lower half before its upper, gravel never onto air), and `walkLayer()` refuses to reorder a layer that writes any coord twice, since a raw model plan encodes place-then-carve intent in that order. Also `bot.swingArm()` before each `/setblock`, since the block lands by command and otherwise has no animation at all. Verified: all four presets keep their exact block multiset, never step y backwards, never change which write lands last; live `npm run replay` placed 1326/1326 with `verifyBuild` reporting 0 missing. (2) **The edge of the world.** The hard horizon where the grass stopped was the edge of what got *drawn*, not of the platform: the server streamed the vanilla default of 10 chunks (~160 blocks) around the camera, and the scene platform is 337x337, so its far half was never sent. Server `VIEW_DISTANCE` 10 -> **24** (384 blocks, `MEMORY` 1G -> 2G to back it) and `VIEWER_DISTANCE` 8 -> **16**; both ceilings had to move, since the browser renders the bot's world and cannot draw chunks the server never sent. Docker only reads `-e` at container CREATE, so a changed server setting silently did nothing on `stop`+`start` and the only command that rebuilt the container (`server:reset`) also deleted the world - new **`npm run server:recreate`** rebuilds the container and keeps the world volume~~ ✅ done 2026-07-13
- [x] ~~**Fix: "why is there a floating piece of wood?" - the build was fine, the VIEW of it was lying** - a finished Willowbrook Cottage rendered in the browser as a roofless house with its ridge beam hanging in mid-air. Reading the world back proved the roof was entirely there and correct (89 spruce stairs + 10 ridge slabs, exactly what the plan asked for) and `verifyBuild` reported 0 missing - the bug was never in the build, only in the picture of it. Root cause: prismarine-viewer re-sends (unloads + reloads) chunk columns every time the bot it is bound to crosses a chunk boundary, and meshes chunk sections asynchronously in a worker; if a section's mesh job is in flight when its column is unloaded, `worldrenderer.js` re-checks `loadedChunks` and throws the finished geometry away, and nothing ever re-queues it - the section stays frozen at whatever it last drew. The view was bound to the lead WORKER, and workers teleport around the site constantly, so the sections holding the roof froze mid-build. Instrumented the viewer's own socket.io stream to prove it: **132 chunk unload/reload cycles in a single cottage build**, while every block reached the browser intact (so the data was perfect and the mesher was the liar). Fix: `src/camera.js` - a 5th bot (`Cam`, spectator, `physicsEnabled = false`, needs no op since the lead `/tp`s it) that the viewer binds to and that only ever moves BETWEEN builds. Same measurement after the fix: **0 unloads**. Everything now goes through `attachViewer()` / `Crew.viewerBot()` (crew, web panel, demo, index, e2e), the render distance moved off prismarine-viewer's 6-chunk default since a fixed camera can no longer chase the crew, and `refreshChunks` takes the camera along (the browser renders from ITS world, so refreshing only the crew would have fixed the bots' biome and changed nothing on screen). New `npm run test:viewer` locks it down: it reconstructs the world from the viewer's actual socket stream and asserts 0 mid-build unloads + every placed block present in the browser~~ ✅ done 2026-07-13
- [x] ~~**The web panel boots in the browser, not the terminal** - `npm run web` did open a browser automatically (since 2026-07-11), but it opened it LAST: `main()` awaited `ensureServerUp()` + `assembleTeam()` + `startViewer()` before it ever called `server.listen()`, so the entire ~30-60s startup (waiting for the world to generate, four bots spawning, the world being pacified) could only ever be watched in the terminal, and the browser arrived at an already-finished panel. `main()` now listens and opens the browser FIRST, then runs the boot as three broadcast steps (server / crew / viewer). The page opens on a loading screen that renders those steps live - spinner on the active one, check on the done ones - and tails the same SSE log stream underneath, so a slow step never reads as a hang. Boot state rides inside `publicState()` rather than a separate event, so a browser that connects late or reloads mid-boot resolves to the right screen from a plain `GET /status`; an already-booted reload skips the fade so the overlay never flashes. The viewer iframe holds its `src` until boot completes (the viewer's port isn't even chosen until then, so an eager load just latched it onto the proxy's 502 page). Verified live on a cold boot: page served at t+2s, steps advanced `server=active → crew=active → viewer=active → done` across 26s, `/viewer/` 200 afterwards, `npm test` still green~~ ✅ done 2026-07-13

- [x] ~~**Fix: a bot dropping MID-scene left a half-built scene that reported success** - the same silent-no-op class as the build-time bug, one level up. Three fixes, in order of how much they matter: (1) root cause - mineflayer gives up after 30s without a keepalive, and a server rewriting whole chunk columns under a scene's thousands of fills stops servicing keepalives long before it stops working, so the bot "timed out" against a perfectly healthy server (`checkTimeoutInterval` is now 120s, `MC_TIMEOUT`); (2) `assertLead()` guards every `/fill`, `/fillbiome` and `/setblock` in the panel, turning a dead lead into a loud error instead of hundreds of commands evaporating; (3) `buildScene` retries once on that error - every fill is idempotent, so starting over is always safe - and the layout stamp is only laid AFTER everything else lands, so a half-built scene can never be cached as finished. Verified by kicking the lead bot 8 seconds into a Beach build: "Scene build interrupted - reconnecting and starting over", and the recovered plot passes every probe (stamp, surface, encasing base, far corner, solid shore, both biomes). Building all 7 scenes back-to-back previously dropped 4 bots; it now drops 0~~ ✅ done 2026-07-12
- [x] ~~**`npm test` - the click-to-place raycast is covered** - placement is the one feature that reaches into prismarine-viewer's internals, so it gets the one test. `test/pick-ground.test.mjs` rebuilds the viewer's exact camera in three.js and runs the REAL `pickGround()`, lifted out of the page source in `scripts/web.js` so the test cannot drift from the code it covers - no browser needed. Asserts the load-bearing property (a click dead centre returns the coordinates of the bot the camera is aimed at, which is only true if the unprojection, the ray, the ground plane and the world-coordinate assumption all agree), plus no mirrored axes and that impossible rays are refused rather than reported as wild spots~~ ✅ done 2026-07-12
- [x] ~~**Preset polish: spaced cacti, clamped peaks** - a cactus breaks itself if anything solid is horizontally adjacent, including another cactus, so the desert was quietly losing one now and then to a random collision; cacti are now kept a clear block apart (and dead bushes can't spawn on top of one). Mountain peak fills are clamped to the platform, so no future change to the peak radius can put one back in mid-air over the edge~~ ✅ done 2026-07-12
- [x] ~~**Click a spot in the 3D view to place the next build** - "Pick a spot" arms the viewer; clicking the ground stakes out a glowing outline of the build's footprint and the crew builds THERE instead of the next grid cell ("Auto" returns to the grid). The viewer's webpack bundle keeps its camera in module scope, but it does publish `THREE` on `window` - so the proxy injects a hook that intercepts that assignment and wraps `WebGLRenderer#render`, which is handed the live camera every frame. The page then turns a click into world x/z by unprojecting the cursor and intersecting the ray with the ground plane (the viewer's scene is in real world coordinates - no conversion), so it needs no mesh picking and works on whatever is loaded. Falls back silently to the auto grid if the hook ever stops matching. Marker is laid flush INTO the surface on exactly the footprint `levelSite()` clears, so starting the build erases it for free. Verified live: staked (-116,-16) -> ring present in-world -> cottage built there -> ring gone, placement reset~~ ✅ done 2026-07-12
- [x] ~~**Fix: building on a scene made the scene rebuild itself - destroying every build on it** - `isSceneBuilt` probed the plot at `origin+20`, which is INSIDE build site 0. Once anything was built there the probe saw the castle instead of bare ground, concluded "not built", and re-laid the entire 337x337 platform on top of the user's gallery. All probe points now sit outside the build area (and clear of the shore apron, the snowy pond, and anything decoration writes at ground level). Verified live: built a cottage on Plains, switched to Beach and back - "already built", cottage intact~~ ✅ done 2026-07-12
- [x] ~~**Fix: the beach had holes in it (again) - the shore steps were falling** - each step of the shoreline was laid as a 1-block-thick shelf of the ground block over the step below it, which had been carved down to the lagoon floor. Sand is a GRAVITY block: every shelf was a slab of sand hanging over air, so it fell the moment it was placed and water poured into the gap. Steps are now filled solid from the lagoon floor up to their own height. Verified live: a vertical slice through the shore is sand all the way down to the sandstone core, water only above it~~ ✅ done 2026-07-12
- [x] ~~**Fix: `web.js` never checked whether the lead bot was still alive** - every `/fill`, `/setblock` and `/tp` in the panel goes out as a chat command from the lead, and `bot.chat()` on a disconnected bot is a SILENT no-op. `Crew.executeBuild` calls `ensureAlive()`, but scene construction, site levelling, clearing and markers all bypass it - so a dropped lead made a scene "build" with zero blocks placed, a clean log and no error anywhere. (Found the hard way: a marker that reported success and never appeared in-world.) All five command paths now `ensureCrew()` first~~ ✅ done 2026-07-12
- [x] ~~**Scene fixes across the presets** - (1) Beach: the sea is painted `warm_ocean` over the apron only, so the water is turquoise instead of flat river-navy, while the dry platform keeps the `beach` biome for correct sand and foliage (water colour is a biome tint, same as grass). (2) Snowy: the "frozen pond" was a rectangle spanning the platform's entire width - it's now an actual wobbly-edged circular pond, and the spruce trees no longer stand on the ice. (3) Mountains: peaks were picked with a 6-block edge margin but can be 10 wide, so a peak near the rim spilled off the platform and hung in mid-air - the margin now clears the widest peak. Verified live: all 7 scenes rebuilt clean, turquoise sea + beach sand biomes confirmed at the right coordinates, pond contained, build areas clear~~ ✅ done 2026-07-12
- [x] ~~**"Clear ground" now sweeps hand-placed builds too** - it replayed the 3x3 grid from a build COUNT, which stops describing reality the moment a build can be placed anywhere. The panel now tracks the real origin of every build per plot (`state.sceneSites`) and bulldozes those~~ ✅ done 2026-07-12
- [x] ~~**Fix: "why is the house not finished?" - a bot that dies mid-build fails SILENTLY** - `bot.chat()` on an ended mineflayer bot is a no-op that neither throws nor warns, while `blocksPlaced` keeps counting, so a worker that times out mid-build produced a half-built house *and* a cheerful "Build complete! 779 blocks". Each role's blocks go out in list order and the roof is the TAIL of the carpenter's list, so the symptom was always a roofless house. The server log confirmed it: `Woody lost connection: Timed out` mid-session. Root cause of the timeouts: vanilla broadcasts every command's output to all OPs, and 4 opped bots firing ~40 `/setblock`/second re-broadcast ~160 chat packets/second at each other - `pacifyWorld` now sets `sendCommandFeedback false`. Fixes: `Worker.alive` (set false on `end`) + `buildBlocks` throws if it dies; `Crew.ensureAlive()` reconnects dead workers before a build; `Crew.verifyBuild()` re-reads the world afterwards and logs exactly which blocks never landed. Verified live: cottage 872/872 structural blocks present, roof 131/131~~ ✅ done 2026-07-12
- [x] ~~**Fix: "why do we have all these caverns?" - hostile mobs were eating the scenery** - idle bots keep the plot's chunks loaded, so the game keeps spawning mobs there (70 loaded at once: creepers, endermen, witches); with `mobGriefing` on, endermen lift grass/sand blocks straight out of the surface, leaving the pits that read as caves in the viewer (247 of them on the Plains plot). New `src/world.js` `pacifyWorld(bot)`, called by `Crew.assembleTeam` on the lead bot: peaceful difficulty (which despawns the hostiles already loaded), no mob spawning, no griefing, no fire tick, no weather, permanent noon, no command feedback, and a dropped-item sweep. Verified live: 0 hostile mobs after a crew connect. (Note: creepers never *exploded* the builds - they ignore creative-mode bots - a 6-minute night-time soak test with 65 mobs cost the cottage zero blocks.)~~ ✅ done 2026-07-12
- [x] ~~**Fix: the castle's gate doors drowned (found by the new build-integrity check)** - two compounding bugs, caught by polling the gate coords through a live build: (1) a door is TWO blocks and `/setblock` places one - a lone lower half is an invalid state the game deletes, so every preset's doors were fragile (new `door()` helper places both halves with a facing); (2) the bridge deck under the gate was landscaper work, but so is the moat - the crew airs out the whole site before building, so the water reached the empty ground layer under the gate 26 seconds after the carpenter hung the doors, and a door whose support turns to water breaks instantly. The bridge is now mason work (first role), the landscaper carves clear of it, and the gate-arch carve is bounded to `y >= 0` so it stops eating the deck it stands on (`b.y <= 3` alone also matched y=-1). Live: all four presets now verify 0 missing blocks (Stonewatch Keep 3213, Arcane Spire 1190, Willowbrook Cottage 722, Beacon Point Lighthouse 1667)~~ ✅ done 2026-07-12
- [x] ~~**Fix: "why the gap in the chimney?" - and the whole class of role-overwrite bugs** - `/setblock` REPLACES, and the crew builds mason -> carpenter -> decorator -> landscaper, so a later role silently wins any shared coordinate. That's how windows get carved, but it's also how (1) the cottage's roof stair punched a hole through its own chimney (confirmed in-world: `y=78 want bricks got spruce_stairs`), (2) the landscaper's island buried the lighthouse's base course in grass and gravel, and (3) a scattered flower landing on a wall replaced the wall block and then popped off, leaving a real hole. Fixes: the cottage roof builds *around* the chimney column; the lighthouse grounds carve out the tower footprint (as the landscaper's final act, so the late grass tufts can't sneak back in); and `finalize()` generically drops any pop-off block whose coord an earlier role already claimed. Also fixed: the lighthouse island's gravel sat directly on the water disc and 39 blocks fell through - gravity blocks need solid support. Cross-role overwrites per preset: cottage 10 -> 2, lighthouse 43 -> 11 (the remainder are intentional windows/lanterns). Verified live: cottage 0 missing, lighthouse 0 missing~~ ✅ done 2026-07-12
- [x] ~~**Fix: grass still rendered olive even after the biome was correct ("why is grass ugly yellowish?")** - `/fillbiome` changes the biome on the SERVER but does not re-send the chunks to clients that already hold them. The crew bots connected long before the scene was painted, and the browser viewer renders from the *bot's* world (not the server's), so it kept drawing the old desert tint over correct grass blocks - the few bright-green squares were just the chunks that happened to refresh. Proved it by reading the biome from a freshly-connected bot (plains, id 39) vs the stale crew (desert, id 14). Added `refreshChunks()`: after painting a scene the crew teleports clear of the server's view distance and back, forcing those chunks to unload and be re-sent with the new biome. Also added `state.sceneNonce` so the page re-frames the viewer after a rebuild-in-place (previously it only re-framed when the scene NAME changed, so a rebuilt scene kept the stale mesh on screen)~~ ✅ done 2026-07-12
- [x] ~~**Fix: perfectly-built scenes were being needlessly rebuilt** - `isSceneBuilt` probes three points, but they live in DIFFERENT chunks (the far corner is 150 blocks out, the stamp 30 below), and the wait-loop only waited for the centre one. An unloaded chunk reads `null`, which is indistinguishable from "not built" - so a cached plot got torn down and rebuilt just because a chunk was slow to stream. Now waits for every probe point to load before judging, and treats a still-unreadable point as inconclusive~~ ✅ done 2026-07-12
- [x] ~~**Fix: scenes were the wrong COLOUR and marooned in natural terrain ("why do the plains have different color grasses? why is it surrounded by desert?")** - two causes. (1) Grass/foliage/water colour is a biome TINT, not a block property: `grass_block` placed in the world-spawn desert renders dry olive, and a biome seam crossing the plot shows up as a patch of a different green. Scenes now carry a `biome` and get a `/fillbiome` pass. The first attempt looked like it ran but mostly hadn't - `/fillbiome` enforces the same **32768-block cap** as `/fill`, and the naive 32x32 tiling over the full height was 32*32*66 = 67k per command, so the server silently rejected 4 of every 5 (only the thin edge strips landed). `fillBiome` now splits on volume like `fillRegion`. (2) prismarine-viewer draws ~96 blocks (6 chunks) around the bot, and the platform was smaller than that, so the desert showed past its edge - platform is now 337x337, sized off the render distance rather than the build grid. Added `SCENE_STAMP`, a block buried under each plot recording the layout that built it, so geometry changes reliably invalidate cached scenes (surface-signature probes alone can't). Verified live: 0 rejected `/fillbiome` commands, and grass_block + `plains` biome confirmed at every point on the viewer's render edge~~ ✅ done 2026-07-12
- [x] ~~**Nobler Works banner on the README** - matches the IRONSIGHT/WarGames org convention (banner image + "Built by Nobler Works" + social badges, above the project title). Image committed at repo root as `nobler-works-banner.JPG`; `src` is a repo-relative path so it renders whether the repo lives under `PatrickWiloak` or `NoblerWorks-HQ`~~ ✅ done 2026-07-12
- [x] ~~**Fix: a busy port 3000 silently killed the browser viewer for the whole session** - `scripts/web.js` now picks the first FREE port at/after `VIEWER_PORT` (`findFreePort`) and proxies `/viewer/` to whatever it actually bound, instead of skipping the viewer entirely (which left the embedded iframe broken / redirect-looping). The viewer port is purely internal - the page only ever talks to `:8080`. Verified by deliberately squatting on 3000: viewer rolled to 3001 and `/viewer/` still served the view + socket.io through the proxy~~ ✅ done 2026-07-12
- [x] ~~**Scenes are cached, not rebuilt every time** - each scene now lives on its OWN permanent plot (a row 320 blocks apart from world spawn), so it's constructed once and reused. `buildScene()` teleports to the plot, and only fills if the plot isn't already built - detected in-memory (`state.builtScenes`) or, for a scene built in a PRIOR session, by probing the plot's block signature (surface ground at exactly `STAGE_Y-1` over the encasing support, with a chunk-load wait so a freshly-teleported bot reads real blocks). Per-scene build counts (`state.sceneBuildCounts`) keep each plot's own gallery. Timings verified live: first-ever build ~9s (persists to disk), subsequent switches ~3s with 0 fills. Sped the first build up from ~32s -> ~9s: parallelized the crew teleports (`Promise.all`), halved the fill volume (1-tile air-clear + 1-tile encase since spawn is flat -> 55 fills not 125), and dropped the anti-spam throttle 110ms -> 45ms (~22 cmd/s, tested clean, no spam-kick). Note: the very first touch of a far plot per session can lag a few seconds on cold chunk-load~~ ✅ done 2026-07-11
- [x] ~~**Scenes are now CONSTRUCTED presets, not biome trips** - replaced the `/locate biome` + `/spreadplayers` travel (which dropped the crew into messy real terrain: caves/ravines showing underneath, cluttered kelp/seagrass oceans, water-holed pads) with `buildScene()`: each scene (Plains/Beach/Desert/Mountains/Snowy/Jungle/Cherry Grove) is constructed in place at a fixed anchor (world spawn, `bot.spawnPoint`, fixed `STAGE_Y=72`) as a clean flat platform - air-cleared above, a solid encasing base below (non-gravity `support` block, so NO caves/junk show underneath), a 2-layer themed surface, and edge-only backdrop decoration (beach's calm 2-deep water pool, oak/jungle/cherry trees, cacti+dead bushes, frozen packed-ice pond + snowy spruce, snow-capped stone peaks). Build grid capped 5x5→3x3 to fit the platform. A default Plains scene builds on startup. Verified live: Plains (grass surface, solid stone to y=30, no air pockets), Beach (calm flush water pool, solid sandstone base), and an 831-block cottage built on the beach platform~~ ✅ done 2026-07-11
- [x] ~~**Background travel polish** - (1) the embedded viewer now re-frames the new location on scene change automatically (client reloads the `/viewer/` iframe when `background` changes, since prismarine-viewer locks its camera on the bot's position at connect-time) - no manual page refresh; (2) arriving at a background lays a wide ~105x105 themed pad (sand beach / snow field / grass clearing) via a chunked `fillRegion` helper so the scene reads as the theme immediately and there's generous flat room to build. Verified: beach travel laid 20 sand fills, anchor at (2813,67,46)~~ ✅ done 2026-07-11
- [x] ~~**`npm run web` auto-opens the browser** - once the panel is listening it launches the default browser (macOS `open`, Windows `start`, Linux `xdg-open`, WSL `wslview`→`explorer.exe` with the exit-code-1 quirk handled so no duplicate tabs); `--no-open` / `NO_OPEN=1` opts out. Verified the opener chain live on WSL2~~ ✅ done 2026-07-11
- [x] ~~**Backgrounds + lively builders (web panel)** - (1) Background chips (Home/Beach/Desert/Mountains/Snowy/Jungle/Cherry Grove): `/locate biome` + `/spreadplayers` moves the crew to a real biome and re-anchors the build grid; (2) every build site is leveled first (48x48 air-out + 4-deep platform of the biome's ground block) so builds sit flat on any terrain; (3) builders now WORK the site - creative mode on connect, each bot hops beside the blocks it's placing (every ~8 blocks or when >10 away) and head-tracks each placement; (4) idle crew parks in a spread formation instead of 4 bots stacked on one block. Two travel gotchas fixed: `/spreadplayers` takes ONE player per command (a space-separated name list is a syntax error - spread each bot individually), and it can't find footing in never-generated chunks (pre-teleport the lead bot high above the target to force terrain generation, then spread with a widening radius 16/48/96). Verified live: desert travel + cottage on a 6,201-block sand platform (226 builder teleports during the build), and mountains travel 2,800 blocks out + cottage on an 8,602-block platform carved into the hillside~~ ✅ done 2026-07-11
- [x] ~~**"Clear ground" button in the web panel** - `POST /clear` bulldozes every used build site (plus the current site after a failed build) via chunked `/fill` air slabs and re-grasses the dug-up ground layer, resets the build grid to site 0. Confirm dialog, disabled while busy / when nothing's built. Verified live: cottage built then cleared, server logged 2,580 blocks aired + 1,774 grass re-laid, `built` reset to 0~~ ✅ done 2026-07-11
- [x] ~~**Single-URL web UI** - the control panel now reverse-proxies the prismarine viewer same-origin at `:8080/viewer/` (HTTP + websocket-upgrade tunnel; `startViewer` gained `prefix`/`quiet` options), so `npm run web` prints exactly one URL. Users were opening the bare `:3000` viewer and finding no prompt box. Verified: panel + viewer page + socket.io handshake through the proxy, websocket tunnel, and a full preset build (Willowbrook Cottage, 826 blocks)~~ ✅ done 2026-07-11
- [x] ~~**Web control panel (`npm run web`, `scripts/web.js`)** - persistent-crew HTTP server (built-in `http`, no new deps): a browser page with a prompt box + preset chips + live color-coded build log (SSE) + the viewer embedded in an iframe. Crew connects once and stays online; each build lands on a fresh grid patch. Graceful shutdown disbands the crew (SIGINT/SIGTERM) so quick restarts don't duplicate-login. Verified: page loads, `/status` + `/events` + `POST /build`, preset build (Willowbrook Cottage 827 blocks) and a live wizard-tower build, screenshotted~~ ✅ done 2026-07-11
- [x] ~~Dropped the other roadmap items per request (build queue/voting, more personalities, team competitions) - removed the Roadmap section from the README~~ ✅ done 2026-07-11
- [x] ~~**Live Gemini path verified end-to-end** - first-ever real LLM build: Gemini designed "Arasaka Cyberpunk Spire" (339 blocks) and the crew built it, 0 errors. Fixes needed to get there: (a) preflight `requireApiKey()` was Anthropic-only - now provider-aware (accepts any of gemini/claude/openai/ollama); (b) default Gemini model `gemini-2.0-flash` had free-tier `limit: 0` for this account - switched default to `gemini-flash-latest`; (c) hardened `parsePlan()` with a brace-extraction fallback for models that wrap JSON in prose~~ ✅ done 2026-07-11
- [x] ~~**Fixed the demo clip camera** - was swaying back-and-forth (recorder did `dir *= -1`); now a LOCKED overview during the build + a single-direction reveal orbit at the end. Re-recorded (2,937 blocks)~~ ✅ done 2026-07-11
- [x] ~~**Record a demo clip + embed at top of README** - recorded the crew building Stonewatch Keep (2,936 blocks) via headless Chromium capturing the live browser viewer; two-speed timelapse edit (14x build, 2.2x reveal); `docs/media/crew-castle-timelapse-640x360-11JUL2026.webp` (3.5MB animated WebP)~~ ✅ done 2026-07-11
- [x] ~~**Migrate the whole stack 1.20.4 -> 1.20.1** - prismarine-viewer only supports 1.20.1 exactly; on 1.20.4 block-state IDs shift and the browser view renders wrong blocks (stone bricks showed as beehives). Verified with an in-world rendered palette-grid screenshot: all preset blocks now render correctly~~ ✅ done 2026-07-11
- [x] ~~**Fix: port-open ≠ server-ready** - after `server:reset`, bots connected during world gen and died with EPIPE; `scripts/server.js` now waits for the current boot's `Done (…)!` log line~~ ✅ done 2026-07-11
- [x] ~~**Fix: `npm run play` with an arg crashed on import** - server.js validated its CLI command at import time; moved inside the direct-invocation guard~~ ✅ done 2026-07-11
- [x] ~~**play menu always shows the 4 presets** (with a key you can also type a custom idea); added MIT LICENSE + license field; README fully rewritten with hero clip, badges, FAQ~~ ✅ done 2026-07-11

- [x] ~~**Upgraded the built-in library builds** - gap-free circular masonry (`cyl`/`disc`/`cone` primitives fix the old ring-gapping bug), textured/weathered stone, round castle towers + moat + gatehouse arch, tapered 3-tier wizard tower with a glowing crown, timber-framed cottage with an overhanging roof + fenced garden, and a NEW 4th build: **Beacon Point Lighthouse**. Verified live: lighthouse placed 1161 blocks, zero errors~~ ✅ done 2026-07-11
- [x] ~~**Multi-provider LLM backend** (`src/providers.js`) - Gemini (free tier) / Claude / OpenAI / Ollama auto-detected from whichever key is set; an explicit `LLM_PROVIDER` with no key degrades to the library instead of crashing~~ ✅ done 2026-07-11
- [x] ~~Wire up the browser viewer (`src/viewer.js`) so anyone can watch without owning Minecraft; graceful fallback if `canvas` is missing~~ ✅ done 2026-07-10
- [x] ~~Rewrite README + add `docs/SETUP.md` (copy-paste setup, troubleshooting)~~ ✅ done 2026-07-10
- [x] ~~**End-to-end test against a live Docker server** - `npm run e2e` (1 bot) + `npm run replay` (full crew): bot(s) spawn, viewer serves HTTP 200, tavern built & 252 blocks verified in-world~~ ✅ done 2026-07-10
- [x] ~~**Fix: bots need OP to `/setblock`** - mount `docker/ops.json` (offline UUIDs) via compose; `npm run ops` regenerates it. The itzg `OPS` env does NOT work offline (online PlayerDB lookup fails, server won't boot). #1 silent-failure gotcha~~ ✅ done 2026-07-10
- [x] ~~**Added `npm run replay`** - full 4-bot crew builds a cached tavern with no API key (live-demo / regression path)~~ ✅ done 2026-07-10
- [x] ~~**One-command UX: `npm run play`** (`scripts/play.js` + `scripts/server.js`) - ensures the server is up (plain docker, no compose plugin) then runs the crew; cached tavern with no key, or `npm run play "prompt"` for a live Claude build. Verified cold-start (stopped→auto-start→build) and warm-start paths~~ ✅ done 2026-07-10
- [x] ~~**Fix: pin server to 1.20.4** (itzg defaults to latest = connect mismatch)~~ ✅ done 2026-07-10
- [x] ~~**Fix: viewer port-in-use crash** - `startViewer` now pre-checks the port and degrades gracefully~~ ✅ done 2026-07-10
- [x] ~~Streamline onboarding: `npm run setup`, `npm run server`, `src/preflight.js` friendly errors~~ ✅ done 2026-07-10
